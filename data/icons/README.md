# Dashboard Icons

These PNGs are fetched from `homarr-labs/dashboard-icons`:

https://github.com/homarr-labs/dashboard-icons

Upstream license: Apache-2.0. Refresh this folder from the repo root with:

```sh
bin/dashboard-icons --force
```
